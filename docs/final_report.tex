\documentclass[11pt]{article}

\usepackage[a4paper,margin=1in]{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{longtable}
\usepackage{amsmath,amssymb}
\usepackage{hyperref}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{xcolor}
\usepackage{enumitem}
\usepackage{caption}

\definecolor{kuetblue}{RGB}{31,73,125}
\hypersetup{colorlinks=true, linkcolor=kuetblue, citecolor=kuetblue, urlcolor=kuetblue}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{Machine Learning Laboratory - CSE 4112}
\fancyhead[R]{KUET}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}

\titleformat{\section}{\Large\bfseries\color{kuetblue}}{\thesection.}{0.5em}{}
\titleformat{\subsection}{\large\bfseries\color{kuetblue}}{\thesubsection}{0.5em}{}

\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}

\title{
\vspace{-1cm}
\textbf{\color{kuetblue}\Large KHULNA UNIVERSITY OF ENGINEERING \& TECHNOLOGY}\\[0.2cm]
\large Department of Computer Science and Engineering\\[0.5cm]
\textbf{\Large Machine Learning Laboratory}\\
\textbf{Course Code: CSE 4112}\\[0.5cm]
\textbf{\Large Project Report}\\[0.3cm]
{\color{kuetblue}\Large\textbf{Occlusion-Robust Face Identification Using a\\ Pretrained ArcFace Model}}\\[0.3cm]
\large Submission Date: 06/09/2026
}
\author{}
\date{}

\begin{document}
\begin{titlepage}
\maketitle
\thispagestyle{empty}
\vspace{1cm}
\begin{minipage}[t]{0.48\textwidth}
\textbf{Submitted to,}\\[0.3cm]
\textbf{Dr. Muhammad Aminul Haque Akhand}\\
Professor\\
Department of Computer Science and Engineering\\[0.4cm]
\textbf{Nabil Faiyaz Sadi}\\
Lecturer\\
Department of Computer Science and Engineering
\end{minipage}
\hfill
\begin{minipage}[t]{0.48\textwidth}
\textbf{Submitted by,}\\[0.3cm]
Roll: 2107031, 2107044, 2107046,\\
2107047, 2107057, 2107059\\[0.4cm]
Section: A\\
Year: 4th\\
Semester: 1st
\end{minipage}
\vfill
\begin{center}
Khulna-9203, Bangladesh
\end{center}
\end{titlepage}

\tableofcontents
\newpage

\section{Introduction}

Modern face-recognition systems perform well when a frontal face is clear, well illuminated, and similar to the images used during training. Their performance can fall sharply when identity-bearing facial regions are hidden by a medical mask, sunglasses, a scarf, a cap, or a combination of these, or when lighting and head pose change. This project addresses the problem as \textbf{closed-set face identification}: every test image is assumed to belong to a person already enrolled, and the system returns the corresponding participant ID (e.g.\ \texttt{p07}) rather than merely detecting whether an occlusion is present.

The final system is a consent-based local dataset of 30 enrolled participants, captured under eight covering conditions and two lighting levels, processed through a fixed detect-align-crop pipeline, and classified with a \textbf{pretrained ArcFace (iResNet50) embedding model} paired with a \textbf{RetinaFace} detector. Three experiments were run to determine how much task-specific training improves a pretrained face embedding, and specifically whether occlusion-diverse training data is necessary for that improvement to hold under compound occlusions. The completed system was packaged into a phone-camera live demonstration served from a laptop over the local network.

This report documents what was actually collected and built, compares the approach against closely related published work, and reports the deviations from the original proposal.

\section{Problem Statement and Objectives}

\subsection{Problem Statement}

Given an input image containing a normal or partially occluded face, the system must classify the image as one of 30 enrolled identities. Different occlusions hide different facial regions: a mask removes the nose, mouth, and jawline; sunglasses remove much of the eye region; a cap changes the forehead and hairline; low light reduces visible texture generally. The central question is whether a pretrained face-embedding model, given only a small amount of per-identity data (a few hundred images per person across all conditions combined), can produce identity features that stay discriminative under these conditions without being explicitly retrained on masks or sunglasses from scratch.

\subsection{Objectives and Completion Status}

\begin{enumerate}[label=\arabic*.]
\item Collect a structured face-image dataset covering a range of occlusion and lighting conditions. \textbf{Completed} — 30 participants, 6{,}000 raw images.
\item Capture each participant under eight covering conditions: none, cap, mask, sunglasses, scarf, clear glasses, mask+sunglasses, mask+clear glasses. \textbf{Completed.}
\item Detect, align, crop, resize, and quality-check every face image using one reproducible preprocessing pipeline. \textbf{Completed} — 5{,}988/6{,}000 images (99.8\%) successfully detected and aligned.
\item Split the dataset into train/validation/test sets such that every identity, and where possible every identity--occlusion combination, appears in all three splits. \textbf{Completed.}
\item Evaluate a pretrained ArcFace pipeline with two forms of task-specific training: a trained classification head, and partial backbone fine-tuning. \textbf{Completed} — three experiments, E1--E3.
\item Evaluate overall accuracy, per-participant accuracy, and per-occlusion accuracy with confidence intervals. \textbf{Completed} — extended beyond the original plan to also include full $30\times30$ confusion matrices and per-class/per-occlusion precision, recall, and F1 for every experiment (Section~\ref{sec:results}).
\item Produce a working demonstration that predicts the enrolled participant from a live phone camera. \textbf{Completed} — a browser-based demo streamed from a phone to a laptop over HTTPS on the local network.
\end{enumerate}

\section{Related Research Papers}

Table~\ref{tab:related-research} lists three published works that are directly comparable to this project's recognition approach and that, in a meaningful sense, represent an improvement over the simple pipeline used here: each either engineers a loss function specifically for occluded/masked faces (rather than reusing a generic ArcFace margin), or evaluates at a scale and rigor this laboratory project cannot match. They are used in Section~\ref{sec:comparison} to place this project's results in context, and were chosen independently of the papers already cited in the original project proposal.

\begin{table}[h]
\centering
\caption{Related recognition-method papers used for comparison.}
\label{tab:related-research}
\begin{tabular}{L{3.3cm}L{5.3cm}L{5.7cm}}
\toprule
\textbf{Paper} & \textbf{Main contribution} & \textbf{Relation to this project} \\
\midrule
Boutros et al., ``Self-restrained Triplet Loss for Accurate Masked Face Recognition,'' \emph{Pattern Recognition}, 2022 \cite{boutros2022srt} &
Proposes a triplet loss that explicitly pulls a masked embedding toward its own unmasked embedding during training, instead of relying on a generic identity loss to generalise to masks by accident. &
Directly comparable model family (ArcFace-style backbone) but with a mask-aware loss; represents the natural next step this project did not take, since E1--E3 here all use a plain angular-margin or nearest-centroid classifier with no mask-specific loss term. \\[0.4cm]
Deng et al., ``Masked Face Recognition Challenge: The InsightFace Track Report,'' \emph{ICCV Workshops}, 2021 \cite{insightface2021mfr} &
Reports results from a public challenge built on the same \texttt{insightface}/ArcFace family used in this project, evaluated on masked faces at web scale (millions of identities) with heavy synthetic mask augmentation during training. &
Uses the identical backbone family as this project but at a scale roughly six orders of magnitude larger; shows what large-scale mask-aware augmentation buys over the untouched pretrained embedding this project starts from, and gives an open-set reference point our closed-set, 30-identity result cannot provide on its own. \\[0.4cm]
Din et al., ``A Novel GAN-Based Network for Unmasking of Masked Face,'' \emph{IEEE Access}, 2020 \cite{din2020gan} &
Trains a GAN to remove the mask and inpaint the occluded lower face before passing the reconstructed image to a standard recognition network. &
Represents an alternative, restoration-based strategy (fix the image, then recognise) versus this project's direct-embedding strategy (recognise from the occluded image as-is); a natural extension for a future iteration of this project when the occlusion is severe enough that direct embedding degrades. \\
\bottomrule
\end{tabular}
\end{table}

\section{Related Dataset Papers}

Table~\ref{tab:related-dataset} lists three dataset papers, again chosen independently of the ones already cited in the proposal, that are the closest public analogues to the identity-labelled, occlusion-varied dataset built for this project.

\begin{table}[h]
\centering
\caption{Related dataset papers used for comparison.}
\label{tab:related-dataset}
\begin{tabular}{L{3.3cm}L{5.3cm}L{5.7cm}}
\toprule
\textbf{Dataset paper} & \textbf{Dataset content} & \textbf{Relation to this project} \\
\midrule
Wang et al., ``Masked Face Recognition Dataset and Application,'' \emph{arXiv:2003.09093}, 2020 \cite{wang2020rmfrd} &
Introduces three datasets: a Real-world Masked Face Recognition Dataset (RMFRD, 5{,}000 masked + 90{,}000 unmasked images of 525 identities), a Simulated Masked Face Recognition Dataset (SMFRD, masks synthetically pasted onto existing face datasets), and a small real MFR2 set (269 images, 53 identities). &
Closest identity-labelled masked-face dataset at scale; unlike this project's dataset, mask images in RMFRD are collected opportunistically from the web with no controlled pose/lighting protocol and no matched clean/occluded image pairs per identity, whereas this project fixes pose and lighting per condition for every participant. \\[0.4cm]
Wang et al., ``MLFW: A Database for Face Recognition on Masked Faces,'' \emph{CCBR}, 2021 \cite{wang2021mlfw} &
Extends LFW by synthetically adding masks to existing unconstrained face-verification image pairs, producing a masked face verification benchmark. &
Same synthetic-mask strategy applied to a much larger, uncontrolled identity pool (aimed at open-set verification, not closed-set identification); this project instead collects every occlusion physically and in person, at far smaller scale but with a controlled and reproducible per-identity capture protocol. \\[0.4cm]
Ngan, Grother, and Hanaoka, ``Ongoing FRVT Part 6B: Face Recognition Accuracy with Face Masks Using Post-COVID-19 Algorithms,'' NIST Interagency Report 8331, 2020 \cite{nist2020frvt} &
A large-scale, standardised evaluation of dozens of commercial face-recognition algorithms on masked faces, using digitally applied mask overlays on an operational government photo dataset. &
Establishes the industrial state of the art for masked-face accuracy degradation (commonly a 5--50 percentage-point drop depending on mask shape/colour, algorithm, and threshold); this project's own robustness gap (Section~\ref{sec:results}) is far smaller, but on a dataset that is orders of magnitude smaller and closed-set, so the two results are not directly comparable — a caveat repeated in Section~\ref{sec:limitations}. \\
\bottomrule
\end{tabular}
\end{table}

\section{Dataset}

\subsection{Team and Participants}

Thirty identities (\texttt{p01}--\texttt{p30}) were enrolled, contributed by six team members who each recruited and photographed five people (themselves plus four relatives/acquaintances who gave consent). The full participant-to-photographer mapping is kept in \texttt{metadata/person\_id\_mapping.txt} and is not reproduced here since it is an internal bookkeeping file, not a reportable result.

\subsection{Consent}

Every participant signed a consent form before any photograph was taken. The form explains that this is an academic, non-commercial prototype; that each participant is assigned an anonymous ID (e.g.\ \texttt{p07}) and never labelled by name; that raw images and the identity mapping are restricted to the six project members and never published outside the group; that participation is voluntary and withdrawable; and that a recognisable photo is only shown in the report, presentation, or demonstration for participants who separately tick an optional permission box.

\subsection{Capture Protocol}

Each participant was photographed under eight covering conditions --- \texttt{none}, \texttt{cap}, \texttt{mask}, \texttt{scarf}, \texttt{clearglass}, \texttt{sunglass}, \texttt{mask\_clearglass}, \texttt{mask\_sunglass} --- crossed with two lighting levels, \texttt{regular} and \texttt{low}. This departs from the proposal's plan of five head poses per condition (front/left/right/up/down) captured across three separate sessions; in practice, collection happened in a single sitting per participant, and pose was not separately labelled at capture time (see Section~\ref{sec:deviations}). The realised dataset totals \textbf{6{,}000 images}: 30 identities $\times$ 200 images (25 images per occlusion/lighting cell on average), which comfortably exceeds the proposal's original target of 10--15 participants.

Filenames follow a fixed pattern, e.g.\ \texttt{p07\_mask\_sunglass\_low\_014.jpg}, so identity, occlusion, and lighting can be parsed without a lookup table; a manifest CSV remains the authoritative metadata source.

\subsection{A Note on Synthetic Augmentation}

Beyond the real captured images described above, the training-only augmentation pipeline (Section~\ref{sec:model}) applies mild synthetic transformations --- horizontal flips, small rotations, brightness/contrast jitter, resolution degradation, motion/Gaussian blur, sensor noise, JPEG re-compression, and random erasing --- to simulate the live phone-camera domain the deployed demo runs in. No fully synthetic images (e.g.\ GAN-generated faces or digitally pasted masks, as used by some of the dataset papers in Section~4) were used anywhere in this project; every image an identity is trained or tested on was captured from a real person wearing a real occlusion.

\section{Data Cleaning and Quality Control}

Photos were rejected (and moved to a separate \texttt{put\_asides} folder with a recorded reason, never silently deleted) for the same criteria proposed originally: excessive blur, unusable low light, a face fully covered with no identity-bearing region left visible, bad framing/cropping, or a wrong/missing label. This project's dataset is treated as \textbf{fixed} as of the start of the training phase — no further deletions, restorations, or relabelling were performed once preprocessing began, so every number in this report is computed on the same frozen 6{,}000-image set.

\section{Data Preprocessing}
\label{sec:preprocessing}

Every accepted photo passes through one fixed pipeline (\texttt{scripts/01\_preprocess.py}), identical for training, validation, test, and later, the live demo:

\begin{enumerate}[label=\arabic*.]
\item \textbf{Detect.} RetinaFace (\texttt{insightface}'s \texttt{buffalo\_l} detector, \texttt{det\_10g.onnx}) locates the face and its five landmarks (both eyes, nose, both mouth corners) at \texttt{det\_size=(640,640)}, \texttt{det\_thresh=0.5}. The largest detected face is kept.
\item \textbf{Fallback on failure.} If nothing is detected, the image is retried at a looser \texttt{det\_thresh=0.3}, and if that still fails, at 2$\times$ upscale with the loose threshold. Every failure is logged, never dropped silently.
\item \textbf{Align and crop.} A similarity transform (\texttt{insightface.utils.face\_align.norm\_crop}) maps the five landmarks onto ArcFace's standard reference template, producing a $112\times112$ aligned crop. This step is what prevents the classifier from keying on background or camera artifacts rather than the face.
\item \textbf{Log.} Each image's path, identity, occlusion, lighting, source filename, MD5 hash, detection outcome/score/stage, and crop path are appended to \texttt{metadata/preprocess\_log.csv}.
\end{enumerate}

\textbf{Result:} 5{,}988 of 6{,}000 images (99.8\%) were successfully detected and aligned, comfortably above the 97\% acceptance gate set in the training plan. The 12 failures were concentrated in the hardest occlusion conditions --- 10 in \texttt{mask\_sunglass} and 2 in \texttt{mask} --- where the combination of a covered lower face and (for 10 of them) a covered eye region left too little visible structure for landmark detection even at the loosened threshold.

\section{Data Organisation and Split}
\label{sec:split}

\subsection{Duplicate Handling}

Md5 hashing of every image revealed 429 byte-identical duplicate files (repeat shutter presses, or one photo saved twice during transfer). By project decision, duplicates are \textbf{not} grouped for splitting purposes — each is treated as an independent image, exactly like any other. This is simpler and more transparent than trying to silently deduplicate a dataset that was declared fixed, but it means a small amount of the reported test accuracy comes from an image that is byte-identical to one seen in training. This effect was measured, not assumed: \textbf{$\approx$8\% of test images share an MD5 hash with a training image}, concentrated in a handful of participants (p05, p21--p25). Section~\ref{sec:results} reports both the headline test accuracy and this caveat together, rather than hiding it.

\subsection{Group-Aware Splitting}

Near-duplicate (but not byte-identical) images are a separate and more common risk: a burst of photos taken a few seconds apart, or a low-light image derived by darkening a regular-light one. Two images are grouped together (and therefore forced into the same split) if:
\begin{itemize}
\item they are derived from the same source image (e.g.\ a brightness-adjusted low-light pair), or
\item they belong to the same person \emph{and} the same occlusion, and were captured within a 10-second window of each other, with burst-chains capped at an 8-second total span to prevent one long shooting burst from silently swallowing an entire occlusion condition for a participant.
\end{itemize}
Groups are then assigned, as whole units, to train/validation/test using a deficit-based greedy allocator: the largest groups are placed first, each into whichever split currently has the largest shortfall relative to its 60/20/20 target share, computed independently per (person, occlusion) cell. This avoids the naive failure mode of a simple sequential fill, which was observed during development to produce a wildly unbalanced split (train 24\%, val 58\%, test 18\%) when a single 49-image group was assigned in one step.

\subsection{Coverage Guarantee}

A hard requirement was enforced: \textbf{every one of the 30 participants must appear in all three splits.} A repair pass exists to forcibly move whole groups from train into an under-represented split if the natural allocation leaves any participant below a floor of 2 images per split; in the final run this repair pass was not needed. Per-occlusion coverage within each split was treated as best-effort rather than a hard requirement: 36 of 240 (person $\times$ occlusion) cells are absent from the test split specifically (informational only, not a failure condition).

\subsection{Final Split}

\begin{table}[h]
\centering
\caption{Final train/validation/test split (5{,}988 successfully preprocessed images).}
\begin{tabular}{lrr}
\toprule
\textbf{Split} & \textbf{Images} & \textbf{Share} \\
\midrule
Train & 3{,}546 & 59\% \\
Validation & 1{,}444 & 24\% \\
Test & 998 & 17\% \\
\midrule
\textbf{Total} & \textbf{5{,}988} & 100\% \\
\bottomrule
\end{tabular}
\end{table}

All hard assertions (no group spans two splits; every participant in all three splits) passed on this final assignment.

\section{Model and Training Approach}
\label{sec:model}

\subsection{Selected Models}

\begin{itemize}
\item \textbf{RetinaFace} (\texttt{det\_10g.onnx}) — detection and five-point landmark alignment (Section~\ref{sec:preprocessing}).
\item \textbf{Pretrained ArcFace, iResNet50 backbone} (\texttt{w600k\_r50.onnx}, \texttt{insightface} \texttt{buffalo\_l}) — turns each aligned $112\times112$ crop into a 512-dimensional embedding. This is the primary identification model in every experiment below.
\end{itemize}

RetinaFace and ArcFace were selected, as in the proposal, because they are purpose-built for faces, need far less local data than training from scratch, are light enough to run on both a 2$\times$RTX~3090 remote box and later a modest laptop CPU, and have precedent in prior published work adapting the same family to occluded/masked faces (Section~3). Unlike the proposal, no separate from-scratch CNN baseline was trained: with only 30 classes and a few hundred images per identity, a from-scratch CNN was judged inherently weaker than adapting a large-scale pretrained backbone, and unlikely to add an informative data point — effort was redirected to a more informative fine-tuning ladder (E1--E3 below).

\subsection{Experiments}

Three experiments were run, each answering a specific question about how much task-specific training the pretrained embedding actually needs:

\begin{table}[h]
\centering
\caption{Experiment design.}
\begin{tabular}{L{1cm}L{3cm}L{3.6cm}L{6cm}}
\toprule
\textbf{ID} & \textbf{Name} & \textbf{Backbone} & \textbf{Question} \\
\midrule
E1 & Trained head, clean-only & Frozen + 30-way ArcFace head & The proposal's original baseline: train only on the \texttt{none} condition. \\
E2 & Trained head, mixed-condition & Frozen + 30-way ArcFace head & Does training on all eight occlusion conditions help over E1? \\
E3 & Partial backbone fine-tune & Last 15\% of backbone unfrozen + head & Does adapting backbone weights (not just the head) help further? \\
\bottomrule
\end{tabular}
\end{table}

\textbf{E1/E2} train a 30-way ArcFace-style classification head ($\cos(\theta_y+m)\cdot s$, $s{=}32$) on top of the frozen embedding, with a margin warm-up ($m$: $0\rightarrow0.20$ over 5 epochs) and label smoothing (0.1), using AdamW with a cosine learning-rate schedule and early stopping on validation macro-F1 (patience 7). E1 trains on \texttt{none}-condition images only; E2 trains on all eight conditions.

\textbf{E3} unfreezes the final 15\% of the ArcFace backbone's parameters (by parameter count) and fine-tunes them jointly with the head at a much smaller learning rate ($10^{-5}$ vs.\ $10^{-3}$ for the head), while keeping every BatchNorm layer in \texttt{eval()} mode throughout — including during training — since letting BatchNorm running statistics update on only $\sim$3{,}500 images was identified as a known failure mode that silently destroys pretrained features. Because \texttt{insightface} ships ArcFace only as an ONNX graph, the backbone was converted to a trainable PyTorch module with \texttt{onnx2torch} and numerically verified against the original ONNX output before training (cosine similarity 1.000000, max absolute difference $1.86\times10^{-4}$), so E3 is a genuine, if approximate, partial fine-tune of the original pretrained weights rather than a reimplementation from a different weight source.

No augmentation was applied to validation or test data at any stage; the live-capture-style augmentations described in Section~4.4 were applied only to E1/E2/E3 training images.

\subsection{A Corrected Bug in E1/E2}
\label{sec:bugfix}

While preparing the full evaluation below, the E1/E2 head script was found to feed \texttt{insightface}'s raw embeddings directly into the angular-margin head under the assumption that they were already unit-length. They are not: \texttt{get\_feat()} returns un-normalised vectors, so the head's $\cos(\theta)=x\cdot w$ computation was in fact an unbounded dot product, not a true cosine, corrupting the $\arccos$ margin term for every training and evaluation batch. The same class of bug was already caught and fixed in the deployment gallery script (Section~\ref{sec:deployment}) but had not been ported back to E1/E2. Once an explicit L2-normalisation of the input embedding was added, E1 and E2 were retrained from scratch with identical hyperparameters and seeds; \textbf{all E1/E2 numbers in this report are from the corrected run}. The effect was large: E1 rose from 70.34\% to 94.29\%, and E2 rose from 96.29\% to 99.90\%, changing the qualitative conclusion from a bug-limited, uninterpretable pair of numbers into a coherent, mechanistically explainable result: E1 fails specifically on the hardest compound occlusion (Section~\ref{sec:results}), and E2 fixes exactly that failure. This is a useful lesson in its own right about validating normalisation assumptions on any embedding used outside its own library's classification code.

\section{Results}
\label{sec:results}

Beyond top-1 accuracy and macro-F1, every experiment below was re-evaluated with a dedicated script (\texttt{scripts/10\_full\_eval.py}) that additionally computes a full $30\times30$ confusion matrix, per-class precision/recall/F1, and per-occlusion precision/recall/F1, persisting all three as reusable JSON/CSV/PNG artifacts under \texttt{runs/eval/<experiment>/} rather than only printing a summary to the terminal.

\begin{table}[h]
\centering
\caption{Test-set results across all three experiments (998 test images, 30 classes, 95\% Wilson confidence intervals). Macro precision/recall/F1 are unweighted means over the 30 per-class values.}
\begin{tabular}{lrrrrl}
\toprule
\textbf{Experiment} & \textbf{Top-1} & \textbf{Macro-P} & \textbf{Macro-R} & \textbf{Macro-F1} & \textbf{95\% CI} \\
\midrule
E1 — Head, clean-only & 94.29\% & 95.06\% & 93.55\% & 93.89\% & [92.7\%, 95.6\%] \\
E2 — Head, mixed-condition & 99.90\% & 99.92\% & 99.92\% & 99.92\% & [99.4\%, 100.0\%] \\
E3 — Partial backbone fine-tune & \textbf{100.00\%} & \textbf{100.00\%} & \textbf{100.00\%} & \textbf{100.00\%} & [99.6\%, 100.0\%] \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[h]
\centering
\caption{Per-occlusion test accuracy across all three experiments, showing exactly where each approach's remaining errors are concentrated.}
\begin{tabular}{lrrrr}
\toprule
\textbf{Occlusion} & \textbf{n} & \textbf{E1} & \textbf{E2} & \textbf{E3} \\
\midrule
none              & 138 & 100.0\% & 100.0\% & 100.0\% \\
cap               & 129 & 100.0\% & 100.0\% & 100.0\% \\
clear glasses     & 108 & 100.0\% & 100.0\% & 100.0\% \\
sunglasses        & 125 & 100.0\% & 100.0\% & 100.0\% \\
scarf             &  84 &  97.6\% & 100.0\% & 100.0\% \\
mask              & 161 &  96.3\% &  99.4\% & 100.0\% \\
mask+clear glasses& 120 &  90.8\% & 100.0\% & 100.0\% \\
mask+sunglasses   & 133 &  \textbf{71.4\%} & 100.0\% & 100.0\% \\
\bottomrule
\end{tabular}
\end{table}

\textbf{E1 vs.\ E2 is the clearest and most informative comparison in this report.} A head trained only on clean, unoccluded faces (E1) drops to 71.4\% specifically on the hardest combined condition, \texttt{mask+sunglasses} — the one occlusion pair that removes both the eye region and the lower face simultaneously — while still scoring perfectly on every single-occlusion condition. The identical head trained on all eight conditions (E2) recovers to 100.0\% on that same condition and 99.90\% overall. This is direct, quantitative evidence that occlusion-diverse training data specifically matters for compound occlusions, not for single occlusions, which a pretrained face embedding already handles reasonably well without any task-specific training at all.

\textbf{E3, partial backbone fine-tuning, reached 100.00\% top-1 and 100.00\% macro-F1}, correctly classifying every one of the 998 test images and achieving perfect precision and recall across all 30 identities and all eight occlusion conditions. Since E2 was already at 99.90\%, E3's gain is small in absolute terms — the honest reading is that E3 closes the last remaining gap rather than solving a problem E2 left substantially open.

\subsection{Confusion Matrices}

Figure~\ref{fig:confusion} shows the row-normalised $30\times30$ test confusion matrix for each experiment. E2 and E3 are visually a clean diagonal with at most one isolated off-diagonal cell; E1's matrix is the only one with a visible, systematic error pattern, concentrated in a handful of identities under \texttt{mask+sunglasses}. The single most common confusion in E1 is participant \texttt{p06} misclassified as \texttt{p16} (6 of 998 test images), followed by \texttt{p16}$\to$\texttt{p11} (5 images) and \texttt{p27}$\to$\texttt{p11} (4 images) — a small number of identities whose combined-occlusion appearance the clean-only head could not separate, resolved entirely once occlusion-diverse training data was used (E2). Full $30\times30$ counts for every experiment are kept in \texttt{runs/eval/<experiment>/confusion\_matrix.csv} rather than reproduced in full here, since a 30-row table adds little beyond what the figure and the discussion above already convey.

\begin{figure}[htbp]
\centering
\begin{minipage}{0.31\textwidth}
\centering
\includegraphics[width=\linewidth]{figures/cm_e1.png}
\caption*{(a) E1 — head, clean-only}
\end{minipage}
\hfill
\begin{minipage}{0.31\textwidth}
\centering
\includegraphics[width=\linewidth]{figures/cm_e2.png}
\caption*{(b) E2 — head, mixed-condition}
\end{minipage}
\hfill
\begin{minipage}{0.31\textwidth}
\centering
\includegraphics[width=\linewidth]{figures/cm_e3.png}
\caption*{(c) E3 — partial backbone fine-tune}
\end{minipage}
\caption{Row-normalised $30\times30$ test confusion matrices for all three experiments. Darker cells indicate a higher share of that identity's test images landing in that predicted column; a perfect classifier is a solid diagonal.}
\label{fig:confusion}
\end{figure}

\subsection{Caveats on the Headline Numbers}

\begin{itemize}
\item \textbf{$\approx$8\% test-training MD5 overlap} (Section~\ref{sec:split}) makes every accuracy above slightly optimistic for the affected participants; the effect is small in absolute terms (E2's 99.90\% has a 95\% CI of only $\pm$0.5 points) but is disclosed rather than hidden.
\item \textbf{Single capture session.} Every split comes from one sitting per participant — same clothing, background, and day. This inflates accuracy relative to a genuine multi-session protocol and is the most significant deviation from the original proposal (Section~\ref{sec:deviations}).
\item With E2 and E3 both at or above 99.9\%, this dataset and task, as collected, does not have enough headroom left to strongly differentiate them from each other; the more informative scientific result in this report is the E1-vs-E2 occlusion-diversity comparison on \texttt{mask+sunglasses}, not the marginal gap between E2 and E3.
\end{itemize}

\section{Comparison with Related Work}
\label{sec:comparison}

Table~\ref{tab:comparison} places this project's result alongside the three related-method papers of Section~3. The comparison is necessarily approximate, since the papers evaluate open-set or web-scale problems that are not the same task as this project's small closed-set identification.

\begin{table}[h]
\centering
\caption{Approximate comparison with related published work. Numbers for related work are as reported in the cited papers; this project's number is E3, this project's own strongest result.}
\label{tab:comparison}
\begin{tabular}{L{3.4cm}L{2.6cm}L{3.2cm}L{5cm}}
\toprule
\textbf{Work} & \textbf{Task} & \textbf{Scale} & \textbf{Reported masked/occluded accuracy} \\
\midrule
This project (E3) & Closed-set identification, 30 identities & 5{,}988 images, 8 occlusion types & 100.00\% top-1 (998/998 test images correctly classified, 95\% CI [99.6\%, 100.0\%]) \\
Boutros et al.\ 2022 \cite{boutros2022srt} & Open-set verification & Standard large face-verification benchmarks with masks added & Reports verification accuracy in the high-90s\% on masked benchmarks, an improvement of several points over a plain ArcFace baseline \\
InsightFace MFR Challenge 2021 \cite{insightface2021mfr} & Open-set verification / identification & Millions of identities, web-scale & Top challenge entries report masked-face TAR in the 90s\% at very low FAR; substantially harder open-set setting than this project's closed set \\
NIST FRVT Part~6B 2020 \cite{nist2020frvt} & Verification, commercial algorithms & Large government-operational dataset & Masked-face accuracy typically drops 5--50 percentage points relative to unmasked, depending on algorithm and mask type \\
\bottomrule
\end{tabular}
\end{table}

The key qualitative takeaway is not that this project "beats" these papers — it does not, in any sense that survives the scale and open-set-vs-closed-set difference — but that a closed-set task with only 30 enrolled identities is dramatically easier than the open-set, web-scale problems these papers address, which is exactly why a pretrained embedding with no occlusion-specific loss engineering (unlike \cite{boutros2022srt}) or synthetic mask augmentation at scale (unlike \cite{insightface2021mfr}) was already sufficient here. This project's contribution is not a new method, but an empirical demonstration of \emph{how far} an off-the-shelf pretrained model goes in a small, tightly-scoped identification setting, and where the remaining gap actually is (E1 vs.\ E2, the occlusion-training comparison, rather than the much smaller E2-vs-E3 gap).

\section{Deployment — Live Demonstration}
\label{sec:deployment}

\subsection{Model Selection and Live Switching}

The demo runs a dropdown that selects between E1, E2, and E3 live, without restarting the server — each request runs full detection, alignment, embedding, and classification for whichever experiment is currently selected. \textbf{E2 (head, mixed-condition) is the default}, since it is close to E3's accuracy (99.90\% vs.\ 100.00\%) without needing E3's heavier, CPU-converted backbone to be loaded first. A per-identity mean-embedding \emph{gallery} classifier with a calibrated \texttt{UNKNOWN}-rejection threshold (0.3179, from the 1st percentile of genuine train-centroid-vs-held-out-validation cosine-similarity scores) is implemented in the codebase (\texttt{scripts/07\_build\_gallery.py}) but is currently disabled in the demo dropdown; consequently, E1/E2/E3 are all closed-set only in the live demo as it stands — every detected face is assigned one of the 30 enrolled identities, with no mechanism yet active to flag an unenrolled visitor.

\subsection{Live Phone-Camera Demo}

The demo runs as a Flask web server on a laptop, serving a browser page to a phone over the local Wi-Fi network via HTTPS (a self-signed certificate, generated once and reused across restarts). The phone's camera streams frames to the server roughly every 400\,ms via \texttt{getUserMedia} (900\,ms when E3 is selected, since its converted backbone is heavier on CPU); each frame is detected, aligned with the identical \texttt{align\_crop} function used in preprocessing, embedded with whichever model is currently selected, averaged over a short rolling buffer of recent frames for temporal stability (cleared on every mode switch, since E3 uses a different embedding space from E1/E2), and classified. The result — participant name/ID and similarity score — is rendered back on the phone screen in real time. A device/camera-lens picker is included so the operator can choose a sharper physical camera on phones with multiple rear lenses (e.g.\ avoiding an ultra-wide lens), and a front/back camera toggle is provided.

\section{Deviations from the Proposal}
\label{sec:deviations}

\begin{table}[h]
\centering
\caption{Deviations between the original proposal and the completed project.}
\begin{tabular}{L{3.6cm}L{4.6cm}L{5cm}}
\toprule
\textbf{Proposal said} & \textbf{What was actually done} & \textbf{Why} \\
\midrule
Three capture sessions (train/val/test each its own session) & One capture session per participant; group-aware stratified split used instead & Time constraints during collection allowed only one sitting per person \\
Accuracy reported by head pose (front/left/right/up/down) & Pose-level breakdown dropped & Pose was not separately labelled during capture \\
Conditions: mask+hat, generic ``partial object'' & Conditions actually collected: none, cap, mask, scarf, clear glasses, sunglasses, mask+sunglasses, mask+clear glasses & Reflects what was practically collectable; clear-glasses conditions were added instead \\
10--15 participants & 30 participants & Six team members each recruited five people instead of a shared, smaller pool \\
From-scratch CNN baseline for comparison & Dropped entirely & With only 30 classes and a few hundred images per identity, a from-scratch CNN was judged inherently weaker than adapting a pretrained backbone, and less informative than the E1-vs-E2 occlusion-training comparison \\
\bottomrule
\end{tabular}
\end{table}

The single-session change is the most consequential deviation: a single-session split cannot demonstrate generalisation across days, clothing, or setting the way a multi-session protocol would, and the reported accuracies should be read with that in mind (see Limitations, below).

\section{Limitations and Future Work}
\label{sec:limitations}

\begin{itemize}
\item \textbf{Single session.} Train, validation, and test all come from one sitting per participant — same clothing, background, and day — which inflates accuracy relative to a genuine multi-session protocol.
\item \textbf{Closed set only.} Every input is forced into one of 30 identities in the current demo configuration (E1/E2/E3, none of which reject an unenrolled face); a gallery-based classifier with a calibrated rejection threshold exists in the codebase but is currently disabled (Section~\ref{sec:deployment}). No formal open-set evaluation (e.g.\ measuring false-accept rate against genuinely unenrolled people) was performed either way.
\item \textbf{Camera confound.} Each team member photographed their own five people with one phone, so camera/device signature correlates with a specific group of five identities. Training-time augmentation (JPEG re-compression, sensor noise) mitigates but does not eliminate this.
\item \textbf{429 exact-duplicate images, treated as independent.} Roughly 8\% of test images are byte-identical to a training image; this is disclosed in Section~\ref{sec:results} rather than corrected for, per the project's decision to keep the dataset frozen once collection finished.
\item \textbf{30 identities.} Results will not extrapolate to hundreds or thousands of enrolled people, and the closed-set numbers here are not comparable in absolute terms to the open-set, web-scale results of Section~\ref{sec:comparison}.
\item \textbf{No anti-spoofing or liveness detection.} This is a laboratory classification prototype, not an access-control system, and would need a liveness check before any real deployment.
\item \textbf{Future work} suggested directly by Section~3's related work: a mask-aware triplet loss (\cite{boutros2022srt}) or a restoration/de-occlusion front end (\cite{din2020gan}) could be evaluated on a second, genuinely multi-session capture round to test whether either helps once the current dataset's ceiling effect (E2/E3 already at or above 99.9\%) is removed by harder, cross-day, cross-location test data.
\end{itemize}

\section{Conclusion}

This project set out to build and evaluate a closed-set face-identification system robust to real occlusions, using a pretrained ArcFace model. Thirty participants were recruited and consented, and 6{,}000 images were collected across eight occlusion conditions and two lighting levels — exceeding the original scale target. A reproducible detect-align-crop pipeline processed 99.8\% of the dataset successfully, and a duplicate-aware, coverage-guaranteed split produced a 3{,}546/1{,}444/998 train/validation/test partition. Three experiments, evaluated with full confusion matrices and per-class/per-occlusion precision, recall, and F1 (not only top-line accuracy), showed that training a classification head only on clean faces degrades specifically on the hardest combined occlusion, \texttt{mask+sunglasses} (E1, 94.29\% overall, 71.4\% on that condition), while training on occlusion-diverse data recovers full performance there and 99.90\% overall (E2); partial backbone fine-tuning closes the remaining gap entirely (E3, 100.00\%, perfect on every occlusion condition). The completed system was deployed as a live, phone-camera-driven demonstration accessible over the local network, defaulting to E2 with E1 and E3 selectable live via a dropdown. Compared against three closely related, more sophisticated published approaches, this project's simple pipeline performs surprisingly well precisely because its task — closed-set identification among 30 known people — is fundamentally easier than the open-set, web-scale problems those works address; the honest scientific contribution here is demonstrating how much of that gap a pretrained, off-the-shelf model closes with only lightweight, correctly-implemented task-specific training, and exactly where (occlusion-diverse training data, not further architecture or loss engineering) the remaining gap actually lived.

\begin{thebibliography}{99}

\bibitem{deng2019arcface} J. Deng, J. Guo, N. Xue, and S. Zafeiriou, ``ArcFace: Additive Angular Margin Loss for Deep Face Recognition,'' in \emph{Proceedings of CVPR}, 2019.

\bibitem{deng2020retinaface} J. Deng, J. Guo, E. Ververas, I. Kotsia, and S. Zafeiriou, ``RetinaFace: Single-Shot Multi-Level Face Localisation in the Wild,'' in \emph{Proceedings of CVPR}, 2020.

\bibitem{boutros2022srt} F. Boutros, N. Damer, F. Kirchbuchner, and A. Kuijper, ``Self-restrained Triplet Loss for Accurate Masked Face Recognition,'' \emph{Pattern Recognition}, vol. 124, 108473, 2022.

\bibitem{insightface2021mfr} J. Deng, J. Guo, X. An, Z. Zhu, and S. Zafeiriou, ``Masked Face Recognition Challenge: The InsightFace Track Report,'' in \emph{Proceedings of ICCV Workshops}, 2021.

\bibitem{din2020gan} N. U. Din, K. Javed, S. Bae, and J. Yi, ``A Novel GAN-Based Network for Unmasking of Masked Face,'' \emph{IEEE Access}, vol. 8, pp. 44276--44287, 2020.

\bibitem{wang2020rmfrd} Z. Wang, G. Wang, B. Huang, Z. Xiong, Q. Hong, H. Wu, P. Yi, K. Jiang, N. Wang, Y. Pei, H. Chen, Y. Miao, Z. Huang, and J. Liang, ``Masked Face Recognition Dataset and Application,'' \emph{arXiv:2003.09093}, 2020.

\bibitem{wang2021mlfw} C. Wang, H. Fang, Y. Zhong, and W. Deng, ``MLFW: A Database for Face Recognition on Masked Faces,'' in \emph{Proceedings of CCBR}, 2021.

\bibitem{nist2020frvt} M. Ngan, P. Grother, and K. Hanaoka, ``Ongoing Face Recognition Vendor Test (FRVT) Part 6B: Face Recognition Accuracy with Face Masks Using Post-COVID-19 Algorithms,'' NIST Interagency Report 8331, 2020.

\end{thebibliography}

\end{document}
